meta {
  name: WebSocket Reconnect
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/admin/websocket-reconnect
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # WebSocket Reconnect
  
  Manually triggers a reconnection to the Finnhub WebSocket.
  Use this if the WebSocket connection is lost or needs to be reset.
}
