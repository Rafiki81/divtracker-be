# AWS CDK Environment Variables
# Copy this file to .env and fill in your values
# Then run: source .env

# AWS Credentials (from aws configure)
# CDK_DEFAULT_ACCOUNT will be auto-detected from AWS CLI
# CDK_DEFAULT_REGION will default to us-east-1
export CDK_DEFAULT_REGION="eu-west-1"

# REQUIRED: Finnhub API Key for market data
# Get your free key at: https://finnhub.io/
export FINNHUB_API_KEY="your-finnhub-api-key-here"

# REQUIRED: Finnhub Webhook Secret for real-time price updates
export FINNHUB_WEBHOOK_SECRET="your-webhook-secret-here"

# REQUIRED: JWT Secret for authentication (64+ characters recommended)
export JWT_SECRET="your-super-secret-jwt-key-min-64-characters-long"

# OPTIONAL: Google OAuth2 for social login
# Get credentials at: https://console.cloud.google.com/apis/credentials
export GOOGLE_CLIENT_ID=""
export GOOGLE_CLIENT_SECRET=""

# OPTIONAL: Firebase Cloud Messaging for push notifications to Android
# Get credentials at: https://console.firebase.google.com
# 1. Go to Project Settings → Service accounts → Generate new private key
# 2. Encode the JSON: cat firebase-key.json | base64 -w0
export FIREBASE_CREDENTIALS_JSON=""
export FIREBASE_PROJECT_ID=""
